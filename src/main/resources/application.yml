server:
  port: 8080
spring:
  task:
    scheduling:
      pool:
        size: 10
  servlet:
    multipart:
      file-size-threshold: 0
  datasource:
    name: tuihoclaptrinh
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      # Automatically commit connections returned from the pool
      auto-commit: true
      # Minimum number of idle connections maintained in the pool
      minimum-idle: 5
      # Database connection timeout, default is 30 seconds, which is 30000
      idle-timeout: 60000
    url: jdbc:mysql://localhost:3306/topic-talks-app?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull
    username: root
    password: p4ssw0rd!
  jpa:
    hibernate:
      ddl-auto: update # If the update data table does not exist, it will be automatically created.
    show-sql: true
    properties:
      hibernate:
        format_sql: true